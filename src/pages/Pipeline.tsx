export default function Pipeline() {
  return (
    <div className="max-w-3xl mx-auto p-6">
      <h1 className="text-2xl font-semibold">Pipeline</h1>
      <p className="text-slate-400 mt-2">Kanban draft → image_pending → pending_approval → approved → scheduled → published. Llega en Fase 2.</p>
    </div>
  )
}
